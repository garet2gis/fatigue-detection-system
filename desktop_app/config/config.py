class LoginConfig:
    def __init__(self, login_url, register_url):
        self.login_url = login_url
        self.register_url = register_url


class AppConfig:
    def __init__(self, url, features_url):
        self.url = url
        self.features_url = features_url
